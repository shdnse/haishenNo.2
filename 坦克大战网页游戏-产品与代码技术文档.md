# 《量子钢潮：终局堡垒》网页游戏

> 产品与代码技术设计文档 / Builder 执行版  
> 文档版本：v0.1  
> 更新日期：2026-08-25  
> 目标读者：Solo Builder、独立开发者、AI 代码生成工具  
> 交付目标：依据本文可直接实现一款完成度高、可部署、可持续扩展的网页坦克游戏

---

## 0. 一页结论

### 0.1 产品一句话

《量子钢潮：终局堡垒》是一款科技废土风的俯视角网页坦克动作 Roguelite：玩家独立控制车身移动和炮塔瞄准，在可破坏战场中守护能源核心、争夺战术节点、组合武器模块，最终击毁占据半屏的多阶段机械巨兽 Boss。

### 0.2 首版必须让玩家感受到的五件事

1. **开火有重量**：后坐力、炮口焰、命中停顿、装甲火花、爆炸冲击波必须同时成立。
2. **坦克不是会移动的人形角色**：车身有惯性，炮塔可独立旋转，正面、侧面、背面受击收益不同。
3. **每局构筑明显不同**：轨道炮、导弹、特斯拉、电浆等路线能形成可感知的流派变化。
4. **战场会变化**：墙体可破坏，能源节点可占领，空投和电磁风暴会改变局面。
5. **Boss 足够震撼且公平**：体型大、部件可破坏、阶段变化清晰，所有致命招式均有预警和躲避窗口。

### 0.3 P0 首版范围

P0 是“可以公开试玩”的最小完整版本，不是仅能演示移动和射击的原型。

| 模块 | P0 内容 |
| --- | --- |
| 游戏模式 | 1 个 12～15 分钟战役模式；通关后解锁同地图无尽模式 |
| 坦克 | 3 台可选底盘：突击、侦察、堡垒 |
| 主武器 | 6 种；每种最多 5 级 |
| 被动模块 | 12 种；每种最多 5 级 |
| 武器进化 | 6 组“满级主武器 + 指定被动”组合 |
| 敌人 | 8 种普通敌人、3 种精英变体 |
| Boss | 1 个巨型 Boss，3 阶段，4 个可破坏部件 |
| 战场 | 1 张大型地图，4 个区域主题，可破坏掩体和占领节点 |
| 随机事件 | 4 种：空投、精英猎杀、电磁风暴、节点争夺 |
| 局外成长 | 轻量科技树，12 个节点；不允许无限数值碾压 |
| 操作 | 键鼠、触屏双摇杆；手柄为 P1 |
| 存档 | 本地存档、设置、最高纪录、解锁状态 |
| 页面 | 启动画面、主菜单、车库、战斗、暂停、结算、设置 |

### 0.4 P0 明确不做

- 不做联网对战、账号、排行榜后端、聊天和好友系统。
- 不做真实弹道学、履带物理或复杂 3D 装甲模型。
- 不做开放世界、剧情对话树、装备背包和商业化系统。
- 不做超过 1 张完整战场和超过 1 个巨型 Boss。
- 不引入 React/Vue 作为战斗层；普通 DOM 只负责页面外壳和辅助 UI。
- 不复制任何现有游戏的名称、坦克造型、地图、角色、图标、音频或界面布局。

---

## 1. 市场玩法拆解与原创取舍

### 1.1 可借鉴的成熟玩法类型

| 玩法类型 | 已验证的乐趣 | 本作采用方式 | 不采用内容 |
| --- | --- | --- | --- |
| 经典基地防守坦克 | 地形阻挡、破墙开路、保护核心、短时决策 | 能源核心防守、砖墙/护盾墙可破坏、草丛隐蔽 | 不复刻经典地图和素材 |
| 现代坦克战术 | 轻/中/重分工、正侧背装甲差异、走位与掩体 | 3 种底盘、方向伤害倍率、炮塔与车身独立 | 不做复杂穿深表、写实军事数值 |
| 双摇杆竞技射击 | 移动与瞄准分离、读弹道、近距离博弈 | WASD/左摇杆移动，鼠标/右摇杆瞄准 | P0 不做真人 PvP |
| 幸存者式 Roguelite | 高频升级、流派成型、强反馈割草 | 经验晶片、升级三选一、武器进化、标签协同 | 不让全武器完全自动，保留主动瞄准 |
| 波次与 Boss Rush | 节奏清晰、阶段考试、长期追求 | 4/8 分钟精英，12 分钟巨型 Boss | P0 只交付 1 个完整 Boss |
| 据点争夺 | 战场目标不只剩杀敌 | 占领能源节点可加速护盾和空投 | 不做复杂团队占点计分 |

### 1.2 设计依据

- 《World of Tanks Blitz》的官方资料将坦克区分为轻型、中型、重型和坦克歼击车，并强调机动、火力、装甲和职责差异；本作将其简化为三种直观底盘，确保网页首局就能理解。其官方规则还展示了“歼灭或占领”的双重目标，因此本作加入能源节点，避免战斗只剩清怪。
- 近年的俯视角坦克作品已反复验证“波次生存 + 模块构筑 + Boss”的组合。例如 Tankron 公开描述了双摇杆/自动瞄准、物品协同、每 5 轮 Boss 和坦克专精；Rogue Tanks 采用随机房间、通关选升级和巨型 Boss；Tank Rogue 强调模块选择、变化战场和突发事件。
- 经典 Battle City 的核心记忆仍是“坦克、地形与防守目标”，但本作只继承玩法语法，不使用其关卡和美术表达。

### 1.3 本作的核心差异点

**差异点 A：守点与游走同时存在。** 能源核心不是要求玩家一直站桩；玩家可以占领远端节点，为核心恢复护盾或触发轨道支援。

**差异点 B：破坏战场形成新路线。** 重炮能打穿墙体，电浆能留下灼烧区，Boss 会摧毁地图结构。每一局的安全路线会改变。

**差异点 C：构筑不仅是数值加法。** 模块会改变弹道、射击节奏和操作方式，例如轨道炮从直线贯穿进化为“折跃棱镜”，导弹从单体追踪进化为蜂群标记。

**差异点 D：Boss 部件破坏影响技能。** 打掉左炮塔不是单纯扣血，而是永久删除一种攻击，同时可能使 Boss 进入更激进的替代行为。

### 1.4 原创与版权边界

- 可借鉴“规则类型”，不可复刻具体地图、角色剪影、UI、音效、文案、数值表和招式演出。
- 坦克统一采用原创“悬浮履带 + 六边形能源核心 + 模块插槽”的科幻造型。
- 暂不使用真实军队徽章、真实坦克名称或受保护的品牌标识。
- 若后续使用第三方素材，必须记录来源、许可证和作者；不能确认授权的素材不得进入正式版本。

---

## 2. 产品定位与体验目标

### 2.1 目标用户

- 喜欢坦克大战、俯视角射击、弹幕躲避和短局 Roguelite 的玩家。
- 希望打开网页即可游玩，不愿下载大型客户端的轻中度用户。
- 键鼠用户是主要对象，触屏用户获得完整但略简化的操作体验。

### 2.2 平台与时长

| 项目 | 目标 |
| --- | --- |
| 平台 | 桌面 Chrome/Edge/Safari/Firefox；移动 Chrome/Safari |
| 单局时长 | 12～15 分钟；首局教学不超过 60 秒 |
| 首次可操作时间 | 正常网络下小于 5 秒，资源较慢时显示真实进度 |
| 目标帧率 | 桌面 60 FPS；中端移动端不低于 45 FPS |
| 显示方向 | 桌面横屏；移动端强制提示横屏 |
| 会话特点 | 无登录、即开即玩、本地保存 |

### 2.3 体验指标

这些指标用于本地 QA 和试玩观察，不要求 P0 接入线上埋点。

| 指标 | 合格线 |
| --- | --- |
| 进入战斗用时 | 从首页点击到可移动不超过 3 次操作 |
| 新手理解 | 30 秒内会移动、瞄准、射击；90 秒内理解升级 |
| 首局存活 | 普通难度首局中位生存时间大于 6 分钟 |
| 构筑差异 | 任意两条进化路线的主要攻击视觉和节奏明显不同 |
| Boss 公平性 | 玩家死亡前能指出导致死亡的招式，而非认为“莫名其妙” |
| 重玩价值 | 连续 3 局可获得不同武器组合或事件顺序 |

---

## 3. 核心游戏循环

```text
主菜单
  → 选择底盘与初始主炮
  → 进入战场，完成 20 秒微教学
  → 清敌 / 守护能源核心 / 占领节点
  → 拾取经验晶片
  → 升级三选一，形成标签协同
  → 遭遇随机事件与精英
  → 12 分钟触发巨型 Boss
  → 击破部件并摧毁核心
  → 结算科技碎片、纪录和新解锁
  → 返回车库，解锁轻量局外科技
```

### 3.1 单局时间轴

| 时间 | 内容 | 设计目的 |
| --- | --- | --- |
| 0:00～0:20 | 移动、瞄准、开火微教学；第一批弱敌 | 无文本墙地完成教学 |
| 0:20～2:00 | 低压波次；首次升级；出现可破坏墙 | 建立核心手感 |
| 2:00～4:00 | 第一战术节点开放；远程敌人加入 | 迫使玩家离开核心区 |
| 4:00 | 精英“护盾破城者” | 第一次构筑检查 |
| 4:30～7:30 | 随机事件；敌人组合化 | 形成路线和资源取舍 |
| 8:00 | 双精英 + 核心受袭事件 | 压力峰值和资源消耗 |
| 8:30～11:30 | 构筑接近完成；Boss 预告逐步增强 | 给予爽感并制造期待 |
| 12:00 | Boss 入场，普通刷怪停止 | 清晰切换终局战斗 |
| 12:00～15:00 | Boss 三阶段 | 机制考试与高潮 |

### 3.2 胜负条件

**胜利：** 巨型 Boss 核心生命归零。

**失败：** 玩家生命归零且无复活机会，或己方能源核心耐久归零。

**保底：** 普通模式每局提供 1 次自动维修，玩家生命归零后原地瘫痪 2 秒，以 40% 生命恢复；Boss 狂暴难度取消该保底。

### 3.3 风险与收益

- 敌人越靠近己方核心，掉落越少，避免玩家只在出生点被动刷怪。
- 占领远端节点可获得一次升级重抽或核心护盾，但会暂时离开防守区域。
- 可破坏补给仓有高价值掉落，同时可能触发伏击。
- 精英的危险招式更强，但必定掉落模块箱。

---

## 4. 操作、镜头与手感

### 4.1 桌面端操作

| 操作 | 默认按键 | 说明 |
| --- | --- | --- |
| 移动 | `WASD` / 方向键 | 车身朝移动方向渐进旋转 |
| 瞄准 | 鼠标 | 炮塔独立朝向世界坐标中的指针 |
| 主炮 | 鼠标左键 | 按住可连续射击；受射速限制 |
| 战术技能 | 鼠标右键 / `E` | 每种底盘不同 |
| 推进闪避 | `Space` | 短距离冲刺，0.25 秒减伤而非完全无敌 |
| 交互/占领 | 自动 | 进入节点范围后自动积累进度 |
| 暂停 | `Esc` / `P` | 冻结战斗，不冻结设置菜单 |

### 4.2 触屏操作

- 左下虚拟摇杆控制移动。
- 右下虚拟摇杆控制瞄准；偏移超过 35% 自动开火，松手停火。
- 右侧两个按钮分别为技能和推进。
- 摇杆只在触点附近出现，最大半径 64 CSS px。
- 触屏默认开启轻度瞄准辅助：准星距敌人小于 48 世界单位时吸附 20%，不得自动转向屏幕外敌人。
- 页面必须阻止战斗区域的滚动、缩放和长按菜单，但设置界面仍应支持正常点击。

### 4.3 坦克运动模型

P0 使用“有坦克感的街机模型”，不实现真实履带物理。

```ts
interface MovementConfig {
  maxSpeed: number;
  acceleration: number;
  deceleration: number;
  hullTurnSpeedDeg: number;
  turretTurnSpeedDeg: number;
  dashSpeed: number;
  dashDurationMs: number;
  dashCooldownMs: number;
}
```

规则：

- 输入方向控制期望速度，实际速度使用 `moveTowards` 平滑追赶。
- 车身角度使用最短角插值，不瞬间转向。
- 炮塔角度独立插值；重型底盘炮塔转速更慢。
- 撞墙时速度衰减到 25%，不能原地抖动或卡入墙体。
- 车身视觉尺寸与碰撞尺寸分离；碰撞体缩至视觉宽高的约 72%。

### 4.4 命中反馈组合

每次有效命中根据武器强度选择反馈等级，禁止所有子弹都触发大幅屏幕震动。

| 等级 | 适用 | 反馈 |
| --- | --- | --- |
| 轻 | 机枪、电弧跳伤 | 小火花、4～8 ms 命中停顿、轻音效 |
| 中 | 标准炮、电浆 | 12～20 ms 停顿、局部闪白、轻微震动 |
| 重 | 轨道炮、爆炸、Boss 部件破坏 | 30～55 ms 停顿、冲击环、色差、明显震动 |

镜头震动上限为 8 px，1 秒内连续震动应合并，设置中可调为 100% / 50% / 0%。

---

## 5. 玩家坦克与数值模型

### 5.1 三种底盘

数值以“突击型”为 1.0 基准，具体像素速度由统一配置表换算。

| 底盘 | 生命 | 速度 | 炮塔转速 | 初始技能 | 玩法定位 |
| --- | ---: | ---: | ---: | --- | --- |
| 突击型 AX-01 | 100 | 1.00 | 1.00 | 过载射击：4 秒射速 +40% | 万金油，首局默认 |
| 侦察型 VX-7 | 72 | 1.28 | 1.20 | 相位推进：冲刺留下一枚诱饵 | 高机动、暴击、风筝 |
| 堡垒型 BG-9 | 145 | 0.76 | 0.78 | 展开护盾：正面获得减伤屏障 | 抗压、近战、守点 |

### 5.2 基础属性

```ts
interface TankStats {
  maxHp: number;
  hp: number;
  armor: number;
  shield: number;
  moveSpeed: number;
  hullTurnSpeed: number;
  turretTurnSpeed: number;
  damageMultiplier: number;
  fireRateMultiplier: number;
  projectileSpeedMultiplier: number;
  critChance: number;
  critMultiplier: number;
  pickupRadius: number;
  cooldownMultiplier: number;
}
```

### 5.3 方向装甲

用“命中方向与车身朝向夹角”计算易伤倍率，给走位带来意义但不造成复杂学习负担。

| 区域 | 夹角 | 玩家受伤倍率 | 敌人受伤倍率 |
| --- | --- | ---: | ---: |
| 正面 | 0°～55° | 0.75 | 0.80 |
| 侧面 | 55°～125° | 1.00 | 1.00 |
| 背面 | 125°～180° | 1.25 | 1.30 |

命中时在坦克旁显示小型文字：`正面格挡`、`侧甲命中`、`背袭 x1.3`。同类提示 500 ms 内合并。

### 5.4 伤害公式

```text
rawDamage = weaponBase
  × (1 + additiveDamageBonus)
  × multiplicativeSynergy
  × directionMultiplier
  × criticalMultiplier
  × difficultyDamageOut

armorReduction = clamp(armor / (armor + 100), 0, 0.65)
hpDamage = max(1, rawDamage × (1 - armorReduction))
```

- 护盾先于生命承伤，护盾不受装甲减伤。
- 暴击默认为 5% 概率、1.75 倍伤害。
- DOT 每 250 ms 结算一次，跳字按 500 ms 合并。
- 同一爆炸对同一目标只造成一次伤害。
- 玩家推进减伤为 65%，不可与其他减伤叠加超过 80%。

---

## 6. 武器、模块与 Roguelite 构筑

### 6.1 升级规则

- 击杀敌人掉落经验晶片，自动被拾取半径吸引。
- 经验满时暂停战斗，随机展示 3 张升级卡。
- 允许 1 次免费重抽；之后每次重抽消耗本局“数据密钥”。
- 已满级项目不再出现；未拥有的主武器最多占 1 个候选位。
- 玩家最多装备 3 个主武器和 4 个被动模块，避免界面和性能失控。
- 关键构筑在第 8 分钟前应有较高概率成型，不能把全部乐趣押到最终 1 分钟。

### 6.2 六种主武器

| 武器 | 标签 | 基础行为 | 升级重点 | 操作感 |
| --- | --- | --- | --- | --- |
| 磁轨主炮 | 动能 / 贯穿 | 朝准星发射高速直线弹，贯穿 1 个目标 | 贯穿、暴击、蓄力 | 慢而重，精确射击 |
| 裂变霰炮 | 动能 / 近战 | 扇形发射 5 枚弹丸 | 弹丸数、击退、近距增伤 | 贴脸爆发 |
| 电浆喷射器 | 能量 / 灼烧 | 连续短射程电浆，留下灼烧地带 | 射程、持续、DOT | 推线与区域控制 |
| 蜂群导弹舱 | 爆炸 / 追踪 | 周期性锁定附近目标发射导弹 | 数量、爆炸半径、标记 | 自动补伤，适合移动 |
| 特斯拉线圈 | 能量 / 连锁 | 对准星附近目标放电并跳跃 | 跳跃次数、眩晕、范围 | 清杂与控制 |
| 守卫无人机 | 机械 / 召唤 | 两架无人机环绕并自动射击 | 数量、同步齐射、维修 | 低操作辅助流 |

### 6.3 十二种被动模块

| 模块 | 标签 | 每级效果示例 |
| --- | --- | --- |
| 超导炮膛 | 射速 | 射速 +8% |
| 钨芯弹链 | 动能 | 动能伤害 +12% |
| 相位电容 | 能量 | 能量伤害 +12% |
| 纳米弹仓 | 装填 | 冷却 -6% |
| 聚焦镜阵 | 暴击 | 暴击率 +4% |
| 爆轰引信 | 爆炸 | 爆炸范围 +10% |
| 自修复装甲 | 生存 | 每 8 秒恢复 1.5% 最大生命 |
| 反应护盾 | 生存 | 每波开始获得 12 点护盾 |
| 矢量推进器 | 机动 | 速度 +6%，推进冷却 -4% |
| 引力回收器 | 资源 | 拾取半径 +18%，经验 +4% |
| 战术处理器 | 技能 | 技能冷却 -8% |
| 蜂群协议 | 召唤 | 无人机伤害和攻速 +10% |

### 6.4 六组武器进化

进化条件：主武器达到 5 级 + 指定被动至少 3 级 + 击杀精英后拾取“量子核心”。进化替换原武器，不额外占槽。

| 主武器 + 被动 | 进化名 | 行为变化 |
| --- | --- | --- |
| 磁轨主炮 + 聚焦镜阵 | 折跃棱镜炮 | 贯穿后折射一次，弱点命中必定暴击 |
| 裂变霰炮 + 钨芯弹链 | 地震破城炮 | 近距离命中产生扇形冲击波，可快速破墙 |
| 电浆喷射器 + 相位电容 | 恒星熔流 | 灼烧区相连后形成持续扩张的高温带 |
| 蜂群导弹舱 + 爆轰引信 | 天穹蜂群 | 标记 3 个目标后同步齐射，爆炸可连锁引爆 |
| 特斯拉线圈 + 战术处理器 | 雷神矩阵 | 玩家推进结束时释放环形闪电，精英只受减速不受硬控 |
| 守卫无人机 + 蜂群协议 | 六翼机群 | 变为 6 架编队，技能期间组成正面拦截网 |

### 6.5 标签协同

玩家拥有同类标签达到指定数量时激活协同，升级页和 HUD 必须显示进度。

| 协同 | 2 件 | 4 件 |
| --- | --- | --- |
| 动能 | 贯穿 +1 | 对满生命敌人伤害 +25% |
| 能量 | DOT 持续 +20% | 每 8 次能量命中触发超载爆发 |
| 爆炸 | 爆炸半径 +15% | 爆炸有 20% 概率产生二次小爆炸 |
| 机械 | 无人机/召唤物 +1 临时单位 | 召唤物可拦截普通子弹 |
| 生存 | 护盾恢复 +20% | 生命低于 30% 时每波触发一次应急屏障 |

### 6.6 三选一生成逻辑

```ts
function createUpgradeDraft(state: RunState, rng: SeededRng): UpgradeCard[] {
  const pool = allUpgrades.filter((item) => isEligible(item, state));
  const weighted = pool.map((item) => ({
    item,
    weight:
      item.baseWeight
      * synergyWeight(item, state.tags)
      * slotNeedWeight(item, state.loadout)
      * antiRepeatWeight(item.id, state.recentDraftIds),
  }));

  const result = weightedSampleWithoutReplacement(weighted, 3, rng);
  enforceAtMostOneNewWeapon(result, state);
  ensureOneUsefulChoice(result, state, rng);
  return result;
}
```

必须满足：

- 连续两次升级页不能出现完全相同的 3 张卡。
- 第 6 次升级前至少出现 1 次已有主武器强化。
- 空槽存在时可以出现新武器；槽满后绝不出现无法选择的武器卡。
- 稀有度只改变附加小词条，不得让普通卡成为纯陷阱。

---

## 7. 战场、地形与目标

### 7.1 世界尺寸

- 逻辑画布：1280 × 720。
- 战场世界：2560 × 1440。
- 基础网格：64 × 64，共 40 × 22.5；实际地图边缘对齐 64 网格。
- 摄像机跟随玩家并有 8% 方向前瞻；不得让玩家看不见前方大部分威胁。
- 己方能源核心位于地图偏下中心，不放在绝对角落。

### 7.2 四个区域

| 区域 | 地形 | 战术特征 |
| --- | --- | --- |
| 核心堡垒 | 合金墙、维修台 | 安全但资源少；最后防线 |
| 废弃工厂 | 传送带、爆炸桶 | 可引爆连锁伤害 |
| 离子荒原 | 电弧裂缝、低掩体 | 间歇区域伤害，机动收益高 |
| 量子研究区 | 护盾门、数据仓 | 高价值空投与精英事件 |

### 7.3 地形类型

| 地形 | 可破坏 | 阻挡移动 | 阻挡子弹 | 特殊规则 |
| --- | --- | --- | --- | --- |
| 合金墙 | 否 | 是 | 是 | 永久边界和核心建筑 |
| 纳米砖墙 | 是，3 段耐久 | 是 | 是 | 破坏后变为碎片减速区 3 秒 |
| 能量护盾门 | 条件破坏 | 否 | 是 | 占领附近节点后关闭 |
| 残骸低墙 | 是 | 否 | 阻挡普通直射弹 | 重炮和激光可穿透 |
| 离子液面 | 否 | 否 | 否 | 降低转向速度 20% |
| 隐形烟幕 | 否 | 否 | 否 | 降低敌人索敌距离 |
| 爆炸桶 | 是 | 否 | 否 | 延迟 300 ms 爆炸，可连锁 |

### 7.4 能源核心

```ts
interface DefenseCoreState {
  maxHp: 1000;
  hp: number;
  maxShield: 300;
  shield: number;
  shieldRegenPerSecond: number;
  underAttack: boolean;
  lastDamageAt: number;
}
```

- 核心有清晰的世界内血条和 HUD 血条。
- 受到攻击时屏幕边缘以箭头指向核心，并播放一次警报；警报 3 秒内不重复。
- 占领能源节点后立即恢复 15% 护盾，并使护盾恢复速度提高 30%，持续 60 秒。
- 玩家不得用车身永久堵住核心入口；敌人寻路应保留至少两条进攻路线。

### 7.5 随机事件

| 事件 | 触发区间 | 内容 | 奖励 |
| --- | --- | --- | --- |
| 轨道空投 | 3～10 分钟 | 远端出现补给舱，45 秒后消失 | 治疗、数据密钥、稀有模块 |
| 精英猎杀 | 4～10 分钟 | 标记精英逃向地图边缘 | 量子核心、额外科技碎片 |
| 电磁风暴 | 5～9 分钟 | 雷区扫描战场，双方子弹速度 -20% | 风暴中击杀经验 +20% |
| 节点争夺 | 2～9 分钟 | 敌方工程车尝试夺取节点 | 成功防守恢复核心护盾 |

同一局最多触发 2 个事件，且不能与 8 分钟强制精英同时开始。

---

## 8. 敌人、AI 与波次

### 8.1 八种普通敌人

| 敌人 | 首次出现 | 行为 | 玩家对策 |
| --- | ---: | --- | --- |
| 侦察蜂 | 0:10 | 快速靠近，接触爆炸 | 提前点杀、保持距离 |
| 步兵坦克 | 0:20 | 朝核心推进，间歇直射 | 基础目标 |
| 侧翼猎手 | 1:30 | 绕到玩家背面，短连射 | 转动车身保护背面 |
| 火箭平台 | 2:30 | 远距离抛射，有落点预警 | 离开红圈、优先击杀 |
| 护盾工程车 | 3:30 | 为附近敌人提供护盾 | 切后排或范围攻击 |
| 破城重坦 | 4:00 | 缓慢冲向核心，正面高甲 | 打侧背、使用爆炸 |
| 干扰无人机 | 5:30 | 降低炮塔转速并短暂遮蔽 HUD | 优先处理、使用技能 |
| 相位狙击者 | 7:00 | 远端蓄力激光，射后换位 | 观察激光线并躲避 |

### 8.2 三种精英词缀

- **过载：** 攻击频率更高，死亡后产生延迟爆炸；地面有明确倒计时圈。
- **镜像：** 生成两个低血量幻象，真身脚下有细微核心光。
- **堡垒：** 正面减伤提高，但背部弱点发光，鼓励绕后。

同一精英最多 1 个词缀。P1 才允许双词缀。

### 8.3 AI 状态机

```text
Spawn
  → AcquireTarget（玩家 / 能源核心 / 战术节点）
  → Navigate
  → AttackTelegraph
  → Attack
  → Recover
  → Navigate

任意状态 → Stunned → Recover
任意状态 → Dead
```

AI 约束：

- 不为每个敌人每帧重新寻路；按 200～500 ms 分批更新路径。
- 远距离使用网格 A*，近距离使用简单转向和避障。
- 允许小型敌人成组共享路径，降低计算量。
- 如果敌人 2 秒内位移小于 8 px，触发脱困：选择相邻可走节点或短暂忽略软碰撞。
- 敌人不可在玩家 240 px 内生成，亦不可在摄像机可见区域中心突然出现。

### 8.4 波次导演

波次不写成固定敌人列表，而是由威胁预算生成。

```ts
interface SpawnDirectorConfig {
  threatPerMinute: number[];
  maxAliveByMinute: number[];
  spawnIntervalRangeMs: [number, number];
  enemyThreatCost: Record<EnemyType, number>;
  compositionRules: CompositionRule[];
}
```

```text
本波威胁预算 = 基础预算 × 时间倍率 × 难度倍率 × 表现修正

表现修正：
- 玩家生命 < 30%：0.88
- 核心生命 < 30%：0.85
- 玩家 30 秒无受伤且清怪很快：1.08
- 同时满足多个条件时限制在 0.80～1.12
```

导演只能温和调节压力，不能暗中修改单个敌人的伤害或让玩家感觉“游戏作弊”。

### 8.5 组合规则

- 火箭平台出现时，近战单位数量不得超过当前波次的 55%。
- 相位狙击者最多同时 2 个。
- 护盾工程车不能互相无限套盾；同一目标只取最高盾值。
- 破城重坦出现前至少提前 1.2 秒播放入口警报。
- 同屏精英最多 2 个，Boss 战时清除尚未激活的精英生成计划。

---

## 9. 巨型 Boss：天穹吞噬者“厄瑞玻斯”

### 9.1 视觉概念

Boss 是一座移动的六足攻城平台，宽度约 620 世界单位，约占桌面可视宽度的 48%。中央为发光反应堆，左右各有轨道炮塔，前部有护盾发生器，后部有无人机舱。整体不是传统真实坦克，而是“移动城市 + 量子反应堆”的原创科幻机械。

### 9.2 部件与本体

| 部件 | 独立生命 | 被摧毁后的影响 |
| --- | ---: | --- |
| 左轨道炮塔 | 900 | 删除左侧扇形炮击，Boss 转向左侧变慢 |
| 右轨道炮塔 | 900 | 删除右侧扇形炮击，Boss 转向右侧变慢 |
| 前部护盾器 | 700 | 本体正面减伤从 65% 降为 20% |
| 后部无人机舱 | 650 | 停止补充无人机；立即掉落治疗包 |
| 中央反应堆 | 5000 | 本体生命，第三阶段持续暴露 |

部件伤害同时以 35% 比例传递给本体，避免玩家认为攻击部件“浪费输出”。

### 9.3 Boss 状态机

```text
Entrance（入场演出，玩家可移动但不可造成伤害）
  → Phase1_Siege（100%～70%）
  → Transition1（震开玩家，掉落补给）
  → Phase2_Hunt（70%～35%）
  → Transition2（摧毁地形，核心暴露）
  → Phase3_Meltdown（35%～0%）
  → DeathSequence
```

### 9.4 第一阶段：封锁协议（100%～70%）

目标：教玩家观察预警、理解部件破坏。

| 招式 | 预警 | 行为 | 安全解法 |
| --- | --- | --- | --- |
| 扇形轨道齐射 | 900 ms，地面三段扇区依次变亮 | 左右炮塔交替轰击 | 向未亮扇区移动 |
| 护盾推进 | 700 ms，Boss 前方出现蓝色墙 | 缓慢向前压迫并撞碎掩体 | 绕侧面打炮塔 |
| 无人机投放 | 舱门闪烁 800 ms | 释放 6 架侦察蜂 | 范围武器清理 |

### 9.5 第二阶段：猎杀协议（70%～35%）

目标：提高移动要求，让场地和已破坏部件影响战术。

| 招式 | 预警 | 行为 | 安全解法 |
| --- | --- | --- | --- |
| 磁力牵引 | 1000 ms，中心出现收缩环 | 将玩家缓慢拉向反应堆 | 反向移动或使用推进 |
| 六足冲锋 | 850 ms，路线出现宽箭头 | Boss 沿直线冲锋并撞墙 | 横向离开，利用后摇攻击背部 |
| 追猎激光 | 650 ms，细线跟随准星后锁定 | 激光扫过锁定方向 | 线锁定后反向移动 |
| 修复无人机 | 舱门绿色闪烁 900 ms | 若机舱未毁，为存活部件恢复 8% | 优先击毁无人机或机舱 |

### 9.6 第三阶段：反应堆熔毁（35%～0%）

目标：短促、强烈、可读，不拖成长时间血牛战。

- Boss 固定在场地中央，反应堆持续暴露，所有部件停止修复。
- 场地边缘出现向内推进的离子风暴，最终安全区仍不小于 700 × 500。
- 每 4 秒释放一圈带缺口的弹幕；缺口宽度不小于玩家碰撞直径的 1.6 倍。
- 每两轮弹幕后触发十字激光，预警 750 ms。
- Boss 生命低于 10% 时音乐升层、画面色温转暖，但不再新增机制。

### 9.7 公平性硬规则

- 致命招式预警不得低于 650 ms；普通难度推荐 800～1000 ms。
- 同一时刻最多存在 2 种主要躲避要求。
- 预警颜色和伤害颜色一致；红色表示伤害，蓝色表示控制，黄色表示可交互弱点。
- Boss 不在屏幕外直接发动无法看见的攻击。
- 阶段转换清除旧弹幕并掉落 1 个小型维修包。
- 玩家被牵引时仍能使用推进，不允许不可逆控制链。
- 接触伤害每个目标有 700 ms 独立冷却，避免一帧多次扣血。

### 9.8 死亡演出

总时长 4～5 秒：

1. 反应堆冻结 300 ms，所有子弹慢速淡出。
2. 四个部件依次爆炸，镜头震动逐级增强。
3. 中央核心坍缩成白点，再释放无伤害冲击波。
4. 屏幕短暂过曝，Boss 残骸落地，显示胜利标题。
5. 允许玩家按键跳过 1.5 秒后的剩余演出。

---

## 10. 模式、难度与局外成长

### 10.1 战役模式：终局堡垒（P0 默认）

- 12 分钟生存 + 3 分钟左右 Boss。
- 普通难度默认开放。
- 完成普通难度解锁无尽模式和狂暴难度。

### 10.2 无尽模式（P0 通关后解锁）

- 不出现最终 Boss 入场；每 5 分钟生成一个强化 Boss 投影。
- 15 分钟后敌人生命、伤害和速度采用软上限增长。
- 只记录本地最高生存时间、击杀和伤害。
- 无尽模式不承担主要平衡目标，P0 以“稳定不崩溃”为验收重点。

### 10.3 难度

| 难度 | 敌人生命 | 敌人伤害 | 预警时长 | 复活 | 奖励 |
| --- | ---: | ---: | ---: | --- | ---: |
| 新兵 | 0.82 | 0.75 | 1.20 | 2 次 | 0.8 |
| 标准 | 1.00 | 1.00 | 1.00 | 1 次 | 1.0 |
| 狂暴 | 1.30 | 1.35 | 0.88 | 0 次 | 1.5 |

难度不改变升级卡质量，不使用“高难度故意给烂卡”的方式。

### 10.4 局外科技树

科技树只提供早期容错和玩法解锁，永久战力总增幅限制在约 20%。

三条分支，每条 4 节点：

- **火控：** 初始暴击 +2% → 初始重抽 +1 → 解锁磁轨主炮 → 首次精英额外掉落。
- **装甲：** 初始生命 +5% → 核心护盾 +5% → 解锁堡垒底盘 → 自动维修恢复提高。
- **机动：** 初始速度 +3% → 拾取半径 +10% → 解锁侦察底盘 → 推进冷却 -5%。

重置科技树免费，避免玩家因早期选择被锁死。

---

## 11. 页面信息架构与 UI

### 11.1 页面状态

```text
Loading
  → MainMenu
      ├─ Hangar / StartRun
      ├─ TechTree
      ├─ Records
      └─ Settings

StartRun → Battle
  ├─ Pause
  ├─ UpgradeDraft
  ├─ GameOver
  └─ Victory → Result → Hangar / Retry
```

### 11.2 首页

- 全屏动态战场背景，降低饱和度和对比，避免抢按钮。
- 左上为游戏标志，副标题“守住最后一座量子堡垒”。
- 主按钮：`开始战役`，视觉层级最高。
- 次按钮：`无尽模式`、`科技树`、`设置`。
- 右下显示版本号、音频开关和全屏按钮。
- 首次访问直接高亮“开始战役”，不弹出冗长公告。

### 11.3 车库

- 中央展示坦克，可轻微旋转或悬浮。
- 左侧选择 3 种底盘，未解锁项展示清晰解锁条件。
- 右侧展示生命、机动、火力、技能，不显示无意义的十几项细数值。
- 底部选择初始主炮，随后点击“部署”。

### 11.4 战斗 HUD

```text
┌─────────────────────────────────────────────────────────────┐
│ [玩家 HP/护盾] [Lv/EXP]                [时间] [暂停]        │
│                                                             │
│  [Boss 名称 / 总血条 / 部件图标，仅 Boss 战显示]            │
│                                                             │
│                        战斗画面                             │
│                                                             │
│ [核心 HP] [事件目标]              [武器冷却] [技能] [推进] │
└─────────────────────────────────────────────────────────────┘
```

- 生命条与护盾条分层；生命低于 30% 才显示屏幕边缘危险效果。
- Boss 条件未触发时不保留空的 Boss HUD。
- 武器自动循环时只显示图标和冷却环；主炮显示准星状态。
- 小地图 P0 不做，改用屏幕边缘目标箭头，降低开发量。
- 战斗跳字默认只显示暴击、背袭、格挡和大额伤害；设置中可开启全部。

### 11.5 升级卡

每张卡的信息顺序固定：

1. 稀有度与类别色条。
2. 图标和名称。
3. 一句话行为，例如“磁轨弹额外贯穿 1 个目标”。
4. 关键数值变化，使用 `20% → 28%`，不写模糊的“显著提高”。
5. 标签与进化提示。

键盘支持 `1/2/3` 选择，`R` 重抽；移动端卡片宽度至少 28vw，点击区域不小于 44 × 44 CSS px。

### 11.6 结算页

先给结果，再给数据：

- 标题：`堡垒已守住` 或 `核心已失守`。
- 显示生存时间、击杀、总伤害、承受伤害、最强武器、Boss 部件破坏。
- 显示获得的科技碎片和新解锁。
- 主按钮 `再次部署`，次按钮 `返回车库`。

---

## 12. 科技风视觉规范

### 12.1 视觉关键词

深空军工、量子能源、全息界面、体积光、硬表面机械、可控霓虹。避免廉价“满屏蓝光”和过量玻璃拟态。

### 12.2 色彩 Token

```css
:root {
  --bg-void: #050812;
  --bg-panel: #0b1324;
  --bg-panel-strong: #101d34;
  --line-grid: #1b3556;
  --text-primary: #edf7ff;
  --text-secondary: #8fa9bd;
  --cyan-energy: #31e6ff;
  --blue-ally: #4a8dff;
  --violet-quantum: #9f67ff;
  --yellow-interact: #ffc857;
  --orange-warning: #ff8736;
  --red-danger: #ff3b5c;
  --green-repair: #4dff9a;
}
```

规则：

- 青色代表玩家能源和主要交互反馈。
- 红色只用于敌对伤害、生命危险和不可站立区域。
- 黄色只用于弱点、稀有掉落和可交互目标。
- 普通正文对背景对比度应达到 WCAG AA 的可读目标。

### 12.3 字体

- 中文：`"Noto Sans SC", "Microsoft YaHei", sans-serif`。
- 数字和标题：可使用 `"Rajdhani"` 或本地系统无衬线替代。
- 不将远程字体设为启动阻塞资源；加载失败必须使用系统字体。
- 战斗内最小正文 14 px，移动端关键数字不小于 16 px。

### 12.4 场景分层

从下到上：

1. 深色地表和低频渐变。
2. 低透明网格、道路与区域标识。
3. 建筑、墙体、可交互物。
4. 地面预警、阴影、轮胎/履带痕迹。
5. 坦克、敌人、Boss 部件。
6. 子弹、激光、粒子。
7. 伤害文字和世界血条。
8. HUD、升级页、暂停页。
9. 全屏后处理和转场。

### 12.5 动效规范

- UI 入场以 160～240 ms 为主；大型结果页可用 360 ms。
- 面板移动使用 `transform` 和 `opacity`，避免频繁触发布局。
- 按钮悬停：上移 2 px、边缘发光增强，不缩放超过 1.03。
- 坦克待机应有轻微能源呼吸，不让整车持续漂浮。
- 重要弱点采用节奏明确的脉冲，普通装饰不与弱点同频闪烁。
- 支持 `prefers-reduced-motion`：关闭色差、强震动和大幅 UI 位移。

### 12.6 美术资产策略

P0 可以使用程序化图形 + 原创 SVG/PNG 占位资产，但最终试玩至少需要：

- 3 台玩家坦克，每台车身和炮塔分层。
- 8 种敌人清晰剪影。
- 1 套 Boss 本体及 4 个可破坏部件。
- 6 种武器图标、12 种模块图标、常用状态图标。
- 地形图块、墙体 3 段破坏状态、爆炸序列或粒子贴图。

所有纹理进入图集；单张战斗纹理建议不超过 2048 × 2048，Boss 可使用独立图集。

---

## 13. 音频规范

### 13.1 音频分组

| 总线 | 内容 | 默认音量 |
| --- | --- | ---: |
| Master | 总音量 | 80% |
| Music | 菜单、战斗、Boss 分层音乐 | 55% |
| SFX | 开火、命中、爆炸、推进 | 75% |
| UI | 按钮、升级、警报 | 65% |

### 13.2 音效优先级

1. Boss 预警和玩家受击。
2. 玩家主炮和技能。
3. 精英攻击。
4. 普通敌人射击。
5. 拾取和环境音。

同种普通音效并发上限 4；超出时复用最早实例。大量爆炸使用 3～5 个音色变体并轻微随机音高，避免机械重复。

### 13.3 浏览器约束

- 首次用户交互后再创建或恢复 AudioContext。
- 页面失焦自动降低音乐并暂停战斗；重新聚焦不自动取消用户主动暂停。
- 切后台后清理持续循环音，恢复时根据当前状态重新建立。

---

## 14. 技术选型

### 14.1 推荐技术栈

| 层 | 选择 | 理由 |
| --- | --- | --- |
| 构建 | Vite + TypeScript | 启动快、配置少、静态部署简单 |
| 游戏引擎 | Phaser 3.90.0 | 成熟稳定，Scene、输入、摄像机、音频、对象池能力完整 |
| 物理 | Phaser Arcade Physics + 自定义弹丸检测 | 适合矩形/圆形的街机俯视角，不需要复杂刚体模拟 |
| UI | Phaser UI + 少量原生 DOM | 战斗 HUD 与游戏同步；菜单和可访问设置可用 DOM |
| 测试 | Vitest | 伤害、抽卡、状态机、存档迁移可脱离画布测试 |
| 格式 | ESLint + Prettier | 让 Solo Builder 输出保持一致 |
| 部署 | 静态站点 | 无服务器依赖，可部署到任意静态托管 |

### 14.2 为什么固定 Phaser 3.90.0

截至本文日期，Phaser 官方归档同时列出 Phaser 4.x 和 Phaser 3.90.0。P0 固定 3.90.0 是为了优先使用成熟的 Phaser 3 生态、示例和类型定义，避免 Builder 在生成过程中混用 Phaser 3/4 API。后续若升级 Phaser 4，应单独建立迁移任务，不能只修改版本号。

### 14.3 不使用纯单文件 HTML 作为正式架构

单文件适合验证手感，但本作包含 Boss 状态机、升级池、地图、存档和大量配置。正式版本必须模块化，否则 Builder 后续修改会产生超长文件、全局状态污染和难以回归的问题。

### 14.4 Phaser 配置

```ts
const config: Phaser.Types.Core.GameConfig = {
  type: Phaser.AUTO,
  parent: "game-root",
  width: 1280,
  height: 720,
  backgroundColor: "#050812",
  pixelArt: false,
  antialias: true,
  physics: {
    default: "arcade",
    arcade: {
      gravity: { x: 0, y: 0 },
      fixedStep: true,
      fps: 60,
      debug: false,
    },
  },
  scale: {
    mode: Phaser.Scale.FIT,
    autoCenter: Phaser.Scale.CENTER_BOTH,
  },
};
```

### 14.5 分辨率与坐标

- 所有战斗逻辑使用 1280 × 720 逻辑分辨率和世界坐标。
- CSS 只负责将画布等比缩放，不允许战斗逻辑读取 CSS 像素作为世界坐标。
- 指针坐标必须通过摄像机转换为世界坐标。
- HUD 使用固定摄像机或独立 UIScene，不随主摄像机滚动。
- 安全区适配刘海屏，移动端关键按钮读取 `env(safe-area-inset-*)`。

---

## 15. 工程结构

```text
quantum-tank-siege/
├─ public/
│  ├─ assets/
│  │  ├─ atlases/
│  │  ├─ audio/
│  │  ├─ fonts/
│  │  └─ ui/
│  └─ favicon.svg
├─ src/
│  ├─ main.ts
│  ├─ game/
│  │  ├─ config.ts
│  │  ├─ events.ts
│  │  ├─ constants.ts
│  │  ├─ scenes/
│  │  │  ├─ BootScene.ts
│  │  │  ├─ MenuScene.ts
│  │  │  ├─ HangarScene.ts
│  │  │  ├─ BattleScene.ts
│  │  │  ├─ UIScene.ts
│  │  │  └─ ResultScene.ts
│  │  ├─ entities/
│  │  │  ├─ PlayerTank.ts
│  │  │  ├─ EnemyTank.ts
│  │  │  ├─ Boss.ts
│  │  │  ├─ Projectile.ts
│  │  │  └─ Pickup.ts
│  │  ├─ systems/
│  │  │  ├─ CombatSystem.ts
│  │  │  ├─ DamageSystem.ts
│  │  │  ├─ SpawnDirector.ts
│  │  │  ├─ UpgradeSystem.ts
│  │  │  ├─ EventDirector.ts
│  │  │  ├─ TargetingSystem.ts
│  │  │  └─ FxSystem.ts
│  │  ├─ boss/
│  │  │  ├─ BossStateMachine.ts
│  │  │  ├─ BossAttacks.ts
│  │  │  └─ BossTelegraphs.ts
│  │  ├─ input/
│  │  │  ├─ InputAdapter.ts
│  │  │  ├─ KeyboardMouseInput.ts
│  │  │  └─ TouchInput.ts
│  │  ├─ map/
│  │  │  ├─ MapBuilder.ts
│  │  │  ├─ NavigationGrid.ts
│  │  │  └─ DestructibleTile.ts
│  │  └─ pools/
│  │     ├─ ProjectilePool.ts
│  │     ├─ EnemyPool.ts
│  │     └─ ParticlePool.ts
│  ├─ data/
│  │  ├─ tanks.ts
│  │  ├─ weapons.ts
│  │  ├─ upgrades.ts
│  │  ├─ enemies.ts
│  │  ├─ waves.ts
│  │  └─ boss.ts
│  ├─ services/
│  │  ├─ SaveService.ts
│  │  ├─ AudioService.ts
│  │  └─ SettingsService.ts
│  ├─ ui/
│  │  ├─ dom/
│  │  └─ styles/
│  ├─ utils/
│  │  ├─ math.ts
│  │  ├─ rng.ts
│  │  ├─ objectPool.ts
│  │  └─ validation.ts
│  └─ types/
│     └─ game.ts
├─ tests/
│  ├─ damage.test.ts
│  ├─ upgradeDraft.test.ts
│  ├─ bossState.test.ts
│  └─ saveMigration.test.ts
├─ index.html
├─ package.json
├─ tsconfig.json
└─ vite.config.ts
```

### 15.1 架构原则

- **数据与行为分离：** 武器、敌人、升级数值放在 `src/data`，禁止散落在 Scene。
- **Scene 只编排：** BattleScene 创建系统并驱动更新，不承担所有伤害、生成和 UI 逻辑。
- **组合优于深继承：** 敌人共享移动/攻击策略，不建立十几层类继承。
- **事件有边界：** 跨 Scene/UI 使用类型化事件总线；同模块内优先直接调用。
- **无循环依赖：** `data → systems → scenes`，反向引用通过接口或事件。

---

## 16. 状态模型与接口

### 16.1 全局状态

```ts
interface AppState {
  version: number;
  settings: GameSettings;
  profile: PlayerProfile;
  currentRun: RunState | null;
}

interface PlayerProfile {
  techShards: number;
  unlockedTankIds: string[];
  unlockedWeaponIds: string[];
  purchasedTechIds: string[];
  records: {
    bestCampaignTimeMs: number | null;
    endlessBestMs: number;
    highestDamage: number;
  };
}
```

### 16.2 单局状态

```ts
interface RunState {
  seed: string;
  mode: "campaign" | "endless";
  difficulty: "rookie" | "standard" | "fury";
  elapsedMs: number;
  level: number;
  xp: number;
  xpToNext: number;
  rerolls: number;
  revives: number;
  loadout: LoadoutState;
  tags: Record<string, number>;
  core: DefenseCoreState;
  stats: RunStats;
  activeEvent: WorldEventState | null;
  boss: BossRunState | null;
}
```

### 16.3 武器配置

```ts
interface WeaponDefinition {
  id: string;
  name: string;
  tags: WeaponTag[];
  iconKey: string;
  maxLevel: number;
  fireMode: "manual" | "auto-nearest" | "auto-pattern";
  cooldownMs: number;
  projectile: ProjectileDefinition;
  levels: WeaponLevelModifier[];
  evolution?: {
    passiveId: string;
    requiredPassiveLevel: number;
    evolvedWeaponId: string;
  };
}
```

### 16.4 伤害事件

```ts
interface DamageEvent {
  id: string;
  sourceId: string;
  sourceTeam: "player" | "enemy";
  targetId: string;
  weaponId: string;
  baseDamage: number;
  damageType: "kinetic" | "energy" | "explosive" | "true";
  hitPoint: Phaser.Math.Vector2;
  hitDirection: Phaser.Math.Vector2;
  canCrit: boolean;
  tags: string[];
  timestamp: number;
}

interface DamageResult {
  amountToShield: number;
  amountToHp: number;
  critical: boolean;
  directionZone: "front" | "side" | "rear";
  blocked: boolean;
  killed: boolean;
}
```

伤害结果只能由 DamageSystem 计算，Projectile 不得直接修改目标生命。

### 16.5 类型化事件总线

```ts
interface GameEventMap {
  "run:started": { seed: string };
  "player:hpChanged": { hp: number; maxHp: number };
  "core:hpChanged": { hp: number; maxHp: number };
  "upgrade:requested": { level: number };
  "upgrade:selected": { upgradeId: string };
  "boss:phaseChanged": { phase: 1 | 2 | 3 };
  "boss:partDestroyed": { partId: string };
  "run:ended": { result: "victory" | "defeat"; stats: RunStats };
}
```

---

## 17. Scene 与暂停规则

### 17.1 Scene 职责

| Scene | 职责 |
| --- | --- |
| BootScene | 加载最小启动资源、显示进度、加载主资源、校验配置 |
| MenuScene | 首页背景、入口、基础设置 |
| HangarScene | 坦克与初始武器选择、科技树入口 |
| BattleScene | 世界、实体、系统、摄像机、战斗计时 |
| UIScene | HUD、事件提示、升级卡、暂停层 |
| ResultScene | 统计、奖励、解锁和重试 |

### 17.2 游戏主状态

```ts
type BattleState =
  | "intro"
  | "playing"
  | "upgrade"
  | "paused"
  | "boss-intro"
  | "boss-fight"
  | "ending"
  | "ended";
```

### 17.3 暂停语义

- `upgrade`：暂停物理、AI、计时器和音效循环；UI 仍可交互。
- `paused`：与升级相同，额外显示设置和退出确认。
- `boss-intro`：暂停玩家开火和伤害，允许有限移动；演出可跳过。
- 页面失焦：自动进入 `paused`，但如果本就处于 `upgrade`，恢复时仍回到升级页。
- 不使用散落的 `scene.time.paused = true`；由统一 PauseController 管理所有时间源。

---

## 18. 碰撞、弹道与对象池

### 18.1 碰撞矩阵

| A | B | 结果 |
| --- | --- | --- |
| 玩家 | 固体地形 | 阻挡 |
| 敌人 | 固体地形 | 阻挡/重新寻路 |
| 玩家 | 敌人 | 软分离；Boss 接触可伤害 |
| 玩家弹 | 敌人/部件 | 伤害、穿透或爆炸 |
| 敌弹 | 玩家/核心 | 伤害 |
| 子弹 | 可破坏墙 | 扣墙体耐久 |
| 拾取物 | 玩家拾取圈 | 吸附并拾取 |
| 预警区 | 任意实体 | 不碰撞，只用于可视化 |

### 18.2 子弹策略

- 普通慢速子弹使用 Arcade Physics 圆形碰撞体。
- 高速磁轨弹使用“上一帧位置到当前帧位置”的线段检测，避免穿透小目标。
- 激光使用射线与目标包围盒求交，不生成大量微型子弹。
- 爆炸先查询半径内候选目标，再逐个计算距离衰减。
- 每枚投射物记录已命中目标集合，贯穿时不重复命中。

### 18.3 对象池

必须池化：普通投射物、爆炸粒子、伤害数字、经验晶片、常见敌人。

```ts
interface Poolable {
  active: boolean;
  reset(...args: unknown[]): void;
  release(): void;
}
```

对象池规则：

- 预热常见对象，首次战斗不得因创建 100 个粒子明显卡顿。
- 到达硬上限时优先复用最旧的低优先级特效，不创建无限对象。
- 实体释放时解绑计时器、事件、Tween 和音频实例。
- 开发模式提供池占用监视器。

---

## 19. 存档与配置迁移

### 19.1 存档介质

P0 使用 `localStorage`，存储键：`quantum_tank_siege_save`。

```ts
interface SaveDataV1 {
  schemaVersion: 1;
  savedAt: string;
  profile: PlayerProfile;
  settings: GameSettings;
}
```

### 19.2 存档规则

- 进入结算页后保存奖励，确保动画中关闭页面也不重复发奖。
- 设置修改后防抖 300 ms 保存。
- 读取时进行运行时校验，不信任 localStorage 内容。
- 数据损坏时保留原字符串到 `quantum_tank_siege_corrupt_backup`，再恢复默认值。
- 提供“导出存档”“导入存档”“清除存档”入口；清除必须二次确认。
- 不保存进行中的 P0 战斗，刷新页面视为该局结束，防止复杂恢复 Bug。

### 19.3 迁移

```ts
function migrateSave(raw: unknown): SaveDataCurrent {
  const parsed = validateBaseSave(raw);
  switch (parsed.schemaVersion) {
    case 1:
      return migrateV1ToCurrent(parsed);
    default:
      return createDefaultSave();
  }
}
```

禁止覆盖旧版本对象原地修改；每次迁移返回新对象并有测试样例。

---

## 20. 性能预算与降级策略

### 20.1 运行时预算

| 项目 | 软上限 | 硬上限 |
| --- | ---: | ---: |
| 同屏普通敌人 | 70 | 100 |
| 活跃玩家投射物 | 220 | 360 |
| 活跃敌方投射物 | 180 | 280 |
| 同屏经验晶片 | 100 | 160，超出自动合并 |
| 活跃粒子 | 500 | 800 |
| 伤害文字 | 28 | 40，超出合并 |
| 动态点光视觉 | 8 | 12 |

### 20.2 必须执行的优化

- 对象池，避免战斗热路径频繁 `new` 和垃圾回收。
- 敌人 AI 分帧更新；不在每帧为所有敌人做 A*。
- 使用空间哈希或网格查询附近目标，避免全量两两比较。
- 经验晶片超过阈值时合并价值，不保留数百个小物体。
- 粒子为纯视觉对象，不加入物理世界。
- UI DOM 每帧最多批量更新一次，不为每点伤害触发多次布局。
- 不在 `update()` 中创建临时数组和闭包；复用向量对象。
- 贴图图集化，音频预解码，首屏资源与战斗资源分批加载。

### 20.3 自动降级

若连续 3 秒平均帧率低于 48：

1. 粒子数量降到 60%。
2. 关闭色差和动态地面痕迹。
3. 伤害数字只保留暴击和大额伤害。
4. 远处敌人的动画更新频率减半。

若恢复到 55 FPS 以上持续 8 秒，可逐步恢复，但不得频繁来回切换。设置中提供低/中/高三档，并允许关闭自动调节。

---

## 21. 可访问性与可用性

- 完整支持键盘完成菜单、升级选择、暂停和重试。
- 可自定义主要按键，至少检测并阻止同一上下文内的键位冲突。
- 提供色盲辅助：敌我除颜色外还有轮廓、图标和形状区别。
- 提供震动强度、闪光强度、伤害数字、屏幕边缘血量效果开关。
- 提供“按住射击/点击切换射击”两种模式。
- 文本不直接烘焙在图片中，便于缩放和后续本地化。
- 移动端竖屏时显示旋转设备提示，不在狭窄画布内强行运行战斗。
- UI 音效、音乐、战斗音效可分别关闭。

---

## 22. 测试策略

### 22.1 单元测试

至少覆盖：

- 方向装甲在边界角度的判定。
- 装甲减伤、护盾吸收、暴击和 DOT。
- 升级三选一不重复、不溢出槽位、满级不再出现。
- 进化条件和标签协同。
- Boss 在 70% 和 35% 阈值只切换一次。
- 存档校验、损坏回退和版本迁移。
- Seeded RNG 相同种子产生相同事件序列。

### 22.2 集成测试

- 从首页选择坦克进入战斗，完成一次升级并暂停恢复。
- 玩家死亡触发自动维修，次数正确减少。
- 核心死亡正确终止生成和伤害。
- Boss 入场清除普通生成计划，阶段转换清除旧弹幕。
- 部件摧毁后对应招式不再出现。
- 结算奖励只发放一次，刷新页面不重复。

### 22.3 手工设备矩阵

| 环境 | 必测内容 |
| --- | --- |
| Windows Chrome/Edge | 键鼠、全屏、失焦暂停、60 FPS |
| macOS Safari/Chrome | 指针坐标、音频解锁、触控板 |
| iPhone Safari 横屏 | 安全区、双摇杆、音频、页面不滚动 |
| Android Chrome 横屏 | 多点触控、性能降级、返回键 |
| 1366×768 笔记本 | HUD 不遮挡、文字可读 |
| 2560×1440 高分屏 | 等比缩放、纹理不糊、准星准确 |

### 22.4 调试面板

开发环境按 `F2` 打开，生产环境关闭。显示：

- FPS、帧时间、活动敌人/子弹/粒子数量。
- 当前时间、威胁预算、下一波和事件。
- 玩家属性、标签、武器 DPS。
- Boss 当前状态、冷却、部件生命。
- 无敌、增加经验、跳到 Boss、直接切阶段、清除敌人按钮。

---

## 23. P0 验收标准

### 23.1 可玩性

- [ ] 玩家可用键鼠和触屏完成移动、瞄准、射击、技能、推进。
- [ ] 车身与炮塔独立旋转，方向装甲判定正确。
- [ ] 3 种底盘均可选，技能和手感有明显差异。
- [ ] 6 种主武器和 12 种模块可正常升级，无死卡或无效卡。
- [ ] 至少 6 组进化都能通过正常流程获得并改变玩法。
- [ ] 8 类敌人行为可区分，不只是换皮和加血。
- [ ] 可破坏墙、能源节点和 4 类随机事件工作正常。
- [ ] 巨型 Boss 有 3 个阶段、4 个部件，招式与部件状态联动。
- [ ] 胜利、失败、重试、返回车库流程完整。
- [ ] 无尽模式在通关后解锁并可稳定运行至少 30 分钟。

### 23.2 视觉与音频

- [ ] 首页、车库、战斗、升级、结算视觉风格统一。
- [ ] 玩家、敌人、友方核心、危险预警不依赖颜色也能分辨。
- [ ] 开火、命中、爆炸、升级、Boss 阶段转换有分层反馈。
- [ ] Boss 入场和死亡演出可跳过且不会破坏状态。
- [ ] 关闭震动、闪光或音频后设置立即生效并被保存。

### 23.3 技术质量

- [ ] `npm run build` 无错误，TypeScript 无隐式 `any` 警告。
- [ ] 核心单元测试通过。
- [ ] 生产构建不暴露调试面板和测试作弊入口。
- [ ] 无明显事件泄漏、计时器泄漏或对象池持续增长。
- [ ] 页面失焦、切后台、旋转屏幕、恢复前台不会加速计时或重复音频。
- [ ] 桌面正常战斗保持约 60 FPS，中端移动设备大部分时间不低于 45 FPS。
- [ ] 本地存档损坏不会导致白屏。
- [ ] 控制台无持续报错、资源 404 和未处理 Promise。

---

## 24. Builder 实施顺序

每个阶段都必须可运行、可检查。禁止一次生成所有内容后再统一修错。

### 阶段 1：工程与灰盒战斗

交付：

- Vite + TypeScript + Phaser 3.90.0 工程。
- Boot、Menu、Battle、UI 基础 Scene。
- 玩家坦克、独立炮塔、鼠标瞄准、主炮、1 种敌人、墙体碰撞。
- 简单 HUD、暂停、重新开始。
- 临时几何图形即可，但必须有后坐力和命中反馈。

完成标准：连续游玩 5 分钟无报错，操作手感通过后再扩展。

### 阶段 2：核心循环

交付：

- 能源核心、经验、升级三选一、对象池、波次导演。
- 3 种敌人、3 种主武器、6 个被动模块。
- 键鼠和触屏输入统一到 InputAdapter。

完成标准：能够从首页打一局到失败并正确结算。

### 阶段 3：内容与地图

交付：

- 3 种底盘、6 种武器、12 种模块、全部进化。
- 8 种敌人、精英词缀、完整地图、可破坏地形、能源节点。
- 4 类随机事件。

完成标准：连续 3 局能形成不同流派，波次压力无明显断层。

### 阶段 4：Boss

交付：

- Boss 状态机、4 个部件、3 阶段全部招式。
- 预警、阶段转换、入场和死亡演出。
- Boss 调试面板和状态测试。

完成标准：所有招式都能在无敌调试模式下独立验证；普通模式可公平通关。

### 阶段 5：表现与页面

交付：

- 科技风首页、车库、升级卡、结算页。
- 正式图集或统一的原创占位美术。
- 音频分组、Boss 分层音乐、设置和可访问选项。

完成标准：无资源 404；弱网有加载反馈；各页面视觉一致。

### 阶段 6：存档、无尽与 QA

交付：

- 科技树、记录、无尽模式、存档导入导出。
- 性能降级、完整测试、生产构建。
- README：安装、开发、构建、部署、控制说明。

完成标准：满足第 23 节全部 P0 验收标准。

---

## 25. 给 Solo Builder 的最终执行指令模板

可将下方内容与本文一起交给 Builder：

```text
请严格依据《量子钢潮：终局堡垒》产品与代码技术设计文档实现项目。

执行原则：
1. 使用 Vite、TypeScript、Phaser 3.90.0；不得把正式项目做成单个超长 HTML。
2. 先完成文档第 24 节的“阶段 1”，运行、检查并报告结果，再继续下一阶段。
3. 所有数值放在 src/data 配置文件；BattleScene 只做编排，不堆积全部业务逻辑。
4. 战斗必须首先保证手感：独立炮塔、后坐力、命中反馈、方向装甲、清晰预警。
5. 所有高频对象使用对象池；伤害统一经过 DamageSystem。
6. 不复制任何现有游戏的素材、名称、地图、UI 或音频。缺少素材时使用风格统一的原创程序化占位图形。
7. 每阶段结束必须运行构建和相关测试，修复控制台错误、资源 404 和类型错误。
8. 桌面端和移动横屏都必须可玩；页面失焦必须安全暂停。
9. 不擅自增加联网、登录、后端、商城、广告和 PVP。
10. 若文档中的视觉目标与性能冲突，优先保证可读性、输入响应和稳定帧率。

每阶段回复时请给出：
- 已完成项目；
- 修改/新增文件；
- 运行与测试结果；
- 当前已知问题；
- 下一阶段计划。
```

---

## 26. 待二次优化的产品决策

这些项目不阻塞 v0.1，但建议在交给 Builder 前由产品方确认：

1. 最终中文名和英文副标题是否保留“量子钢潮：终局堡垒”。
2. 美术更偏“写实军工”还是“霓虹科幻”；本文默认后者。
3. 首发是否必须覆盖手机；本文默认必须，但以横屏为主。
4. 战役时长是 12 分钟还是 15 分钟；本文按 12 分钟触发 Boss。
5. 坦克是使用素材图、原创绘制，还是程序化矢量造型。
6. 是否需要加入中文剧情旁白；P0 默认只有极短系统提示。
7. 是否需要本地双人；P0 默认不做，P1 可考虑同屏合作。
8. 是否需要部署到指定平台和绑定域名。

建议优先优化顺序：**美术方向 → 核心战斗节奏 → Boss 形象 → 移动端范围 → 名称与世界观**。不要在核心手感验证前扩写大量剧情或局外系统。

---

## 27. P1 / P2 扩展路线

### P1

- 第二张地图“轨道升降城”。
- 第二个 Boss“镜面母舰”，主打反射和复制弹道。
- 本地双人同屏合作，倒地救援，共享核心目标。
- 手柄支持、键位重映射。
- 每日种子挑战，本地记录。
- 更多底盘技能和武器进化。

### P2

- 轻量后端排行榜和云存档。
- 异步幽灵挑战，而非实时 PvP。
- 关卡编辑器与玩家自定义波次。
- 赛季化规则变体，但不引入付费数值成长。

扩展前提：P0 的 15 分钟战役稳定、有复玩意愿、性能有余量。没有达到前提时不扩张内容。

---

## 28. 参考与设计依据

以下资料仅用于验证通用玩法结构和技术选型，不代表要复刻其具体表达：

- [World of Tanks Blitz：坦克类型与职责](https://wargaming.net/support/en/products/wotb/article/15403/)
- [World of Tanks Blitz：歼灭与占领胜利条件](https://www.wargaming.net/support/en/products/wotb/article/15401/)
- [World of Tanks Blitz：装甲与弹药机制](https://wargaming.net/support/en/products/wotb/article/15409/)
- [Nintendo：Arcade Archives VS. BATTLE CITY](https://www.nintendo.com/en-ca/store/products/arcade-archives-vs-battle-city-switch/)
- [Tankron：双摇杆、物品协同与周期 Boss](https://store.steampowered.com/app/2121130/Tankron/)
- [Rogue Tanks：随机房间、升级和巨型 Boss](https://store.steampowered.com/app/2918440/Rogue_Tanks/)
- [Tank Rogue：模块构筑、变化战场与突发事件](https://store.steampowered.com/app/4059450/Tank_Rogue/)
- [Phaser 官方版本归档](https://phaser.io/download/archive)
- [Phaser 官方 Arcade Physics 文档](https://docs.phaser.io/phaser/concepts/physics/arcade)

---

## 29. 文档变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v0.1 | 2026-08-25 | 首次建立产品定位、玩法、Boss、UI、架构、数据模型、性能预算与 Builder 执行模板 |

